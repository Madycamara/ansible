user = postfixadmin
password = {{ postfixadmin_password }}
hosts = localhost
dbname = postfixadmin
table = domain
select_field = transport
where_field = domain
additional_conditions = and active = '1'
